function min ( num1, num2 ) {
  if ( num1 > num2 ) {
    return num2;
  } else if ( num1 < num2 ) {
    return num1;
  } else {
    return "cannot answer this";
  }
};

console.log(min(0, "1z0"));
// → 0
console.log(min("0", -10));
// → -10
